# word2pdf bağımlılıkları
#
# click yalnızca terminal arayüzü (CLI) için gereklidir.
click>=8.1.7
#
# Aşağıdakiler YALNIZCA saf-Python yedek motoru (--engine python) için gerekir.
# LibreOffice kuruluysa (varsayılan motor) bunlar olmadan da çalışır.
python-docx>=1.1.0
reportlab>=4.0.0
